name: Build Rovergauge EXE

on:
  workflow_dispatch:
  push:

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v4
        with:
          version: '5.15.2'
          host: 'windows'
          target: 'desktop'
          arch: 'win64_msvc2019_64'
          modules: 'qtcharts'
          cache: true

      # Инициализация среды MSVC без явного указания версии toolset
      # Это позволит использовать версию по умолчанию (VS2026 / v14.4+)
      - name: Setup MSVC Environment
        uses: ilammy/msvc-dev-cmd@v1
        # Параметр toolset удален, чтобы действие само выбрало доступную версию

      - name: Configure CMake
        run: |
          # Проверка переменных окружения
          Write-Host "VSINSTALLDIR: $env:VSINSTALLDIR"
          Write-Host "Qt5_DIR: $env:Qt5_DIR"
          
          # Генератор Visual Studio 17 2022 совместим с более новыми версиями, 
          # но лучше использовать просто "Visual Studio 17 2022" или обновить генератор, если нужно.
          # В среде VS2026 генератор может называться "Visual Studio 18 2026", но 17 2022 обычно работает.
          # Попробуем стандартный вызов.
          cmake -B build -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH="$env:Qt5_DIR"
        shell: pwsh
          
      - name: Build Project
        run: cmake --build build --config Release
          
      - name: Deploy Qt Dependencies
        run: |
          & "$env:Qt5_DIR/bin/windeployqt.exe" --release build/Release/rovergauge.exe
        shell: pwsh

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: rovergauge-built
          path: build/Release/
          retention-days: 5
          if-no-files-found: error
